require "spec_helper"

describe PipedriveApi do
  it "has a version number" do
    expect(PipedriveApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
